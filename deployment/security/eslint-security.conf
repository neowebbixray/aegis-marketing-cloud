module.exports = {
  // ESLint security plugin configuration for Aegis Marketing Cloud
  // https://github.com/nodesecurity/eslint-plugin-security
  plugins: ["security"],
  extends: ["plugin:security/recommended-legacy"],
  rules: {
    // Detect calls to eval()
    "security/detect-eval-with-expression": "error",

    // Detect calls to child_process exec()
    "security/detect-child-process": "warn",

    // Detect non-literal fs calls (e.g., variable paths)
    "security/detect-non-literal-fs-filename": "warn",

    // Detect non-literal require() calls
    "security/detect-non-literal-require": "error",

    // Detect object injection (prototype pollution)
    "security/detect-object-injection": "warn",

    // Detect unsafe regexes (ReDoS)
    "security/detect-unsafe-regex": "error",

    // Detect buffer overflows (no asserts)
    "security/detect-buffer-noassert": "error",

    // Detect new Buffer() usage (deprecated)
    "security/detect-new-buffer": "error",

    // Detect pseudo-random bytes (not cryptographically secure)
    "security/detect-pseudo-random-bytes": "warn",

    // Detect disabled TLS/SSL checks
    "security/detect-disable-mustache-escape": "error",

    // Detect server-side XSS via template injection
    "security/detect-no-csrf-before-method-override": "warn",

    // Detect non-literal regexp
    "security/detect-non-literal-regexp": "warn",

    // Detect possible timing attacks
    "security/detect-possible-timing-attacks": "error",
  },
};
